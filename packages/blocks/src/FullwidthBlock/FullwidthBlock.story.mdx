import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, number, select } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';

import { IOpacity, IAlign } from '../types/BaseBlock';
import FullwidthBlock from './FullwidthBlock';

import imageFile from '../../../../public/preview.png';

<Meta title='Blocks|FullwidthBlock' component={FullwidthBlock} decorators={[withKnobs]} />

# FullwidthBlock

<Preview>
  <Story name="Left">
    <FullwidthBlock 
      image={imageFile}
      headline={text("Headline", "Fully serviced pre-owned moffets for sale and hire")}
      mainBody={text("Main Body", "Munere impetus petentium no nam. Eos cibo mentitum id. Ius an inermis euripidis. Mea et consul bonorum intellegat, vix et percipit lucilius deseruisse, habeo adhuc ubique an nec.")}
      align={select("Align (Default Left)", [IAlign.RIGHT, IAlign.LEFT], IAlign.LEFT)} 
      opacity={select("Opacity", 
        [
          IOpacity.NONE,
          IOpacity.TEN, 
          IOpacity.TWENTY, 
          IOpacity.THIRTY, 
          IOpacity.FORTY
        ], 
        IOpacity.THIRTY)}
      buttonLabel={text("Button Label", "More information here")}
      link={{
        url: "https://hiab.com",
        internal: false
      }}
    />
  </Story>
</Preview>

<Preview>
  <Story name="Right">
    <FullwidthBlock 
      image={imageFile}
      headline={text("Headline", "Fully serviced pre-owned moffets for sale and hire")}
      mainBody={text("Main Body", "Munere impetus petentium no nam. Eos cibo mentitum id. Ius an inermis euripidis. Mea et consul bonorum intellegat, vix et percipit lucilius deseruisse, habeo adhuc ubique an nec.")}
      align={select("Align (Default Right)", [IAlign.RIGHT, IAlign.LEFT], IAlign.RIGHT)} 
       opacity={select("Opacity", 
        [
          IOpacity.NONE,
          IOpacity.TEN, 
          IOpacity.TWENTY, 
          IOpacity.THIRTY, 
          IOpacity.FORTY
        ], 
        IOpacity.THIRTY)}
      buttonLabel={text("Button Label", "More information here")}
      link={{
        url: "https://hiab.com",
        internal: false
      }}
    />
  </Story>
</Preview>

## Props

<Props of={FullwidthBlock} />
