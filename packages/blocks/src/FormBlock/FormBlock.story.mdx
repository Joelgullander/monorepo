import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';

import FormBlock from './FormBlock';

<Meta title='Blocks|FormBlock' component={FormBlock} decorators={[withKnobs]} />

# FormBlock

<Preview>
  <Story name="Regular">
    <FormBlock 
      title={text("Title", "Send me more information about JONSERED XG-H765")}
      mainBody={text("Main Body", "In late ipsum dolor sit amet, consectetur adipiscing elit. Nam laoreet augue augue, in blandit ex rutrum sit The information will be forwarded to the right person that will answer you as soon as possible.")}
      checkboxLabel={text("Checkbox Label", "I approve that Hiab will collect and treat all my personal information in accordance with the GDPR and the Hiab privacy and data protection policies. Read more on hiab.com")}
      buttonLabel={text("Button Label", "Send")}
      checked={boolean("Checked", true)}
      error={boolean("Error", false)}
      errorMessage={text("Error Message", "Something went wrong...")}
      handleChange={action('handle change')}
      handleSubmit={ e => { e.preventDefault(); action('form submitted')(e); }}
    />
  </Story>
</Preview>

## Props

<Props of={FormBlock} />
